CSS FOR FOOD AND BEVERAGE( NOT INCLUDING THE ÄVOID THESE FOODS )

body {
    background: linear-gradient(to right, #f675548a, #63b8eaa8);
}

.header_title {
    font-size: 40px;
    font-family: 'Times New Roman', Times, serif;
}

.menu_bar {
    background-color: #FFFED3;
    height: 80px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 20px; /* Add padding to the sides for spacing */
}

.menu_bar ul {
    list-style: none;
    display: flex;
    align-items: center;
    margin: 0;
    padding: 0;
}

.menu_bar ul li {
    padding: 0 20px;
    position: relative;
}

.menu_bar ul li a {
    color: black;
    text-decoration: none;
    font-size: 20px;
    transition: all 0.3s;
}

.menu_bar ul li a:hover {
    color: white;
}

/* Dropdown specific styles */
.dropdown_button {
    background-color: none;
    color: none;
    padding: 10px 15px;
    border: none;
    cursor: pointer;
}

.dropdown a {
    display: block;
    color: black;
    text-decoration: none;
    padding: 10px 15px;
}

.dropdown .content {
    display: none;
    position: absolute;
    background-color: rgb(246, 246, 246);
    min-width: 100px;
    box-shadow: 2px 2px 5px black;
    z-index: 1;
    
}

.dropdown:hover .content {
    display: block;
}

.dropdown_button:hover {
    background-color: white;
}

.dropdown a:hover {
    background-color: white;
}
.flex-container {
    width: 100%;
    height: auto;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
}

.flex-box {
    width: 200px;
    height: 200px;
    background-color: #BBE9FF;
    font-size: 25px;
    text-align: center;
    line-height: 200px;
    border-radius: 20px;
    margin: 10px;
    transition: transform 0.3s, background-color 0.3s;
}

/* Hover effect */
.flex-box:hover {
    transform: scale(1.1); 
    background-color: #ffffff; 
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3); 
    
}
/* Container styles */


